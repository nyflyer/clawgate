/**
 * Credentials module public API
 *
 * Re-exports types and providers for use by other modules.
 */

export type { CredentialResult, CredentialProvider } from './types'

export { EnvCredentialProvider } from './env-provider'
